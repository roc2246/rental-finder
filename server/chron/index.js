import chron from 'chron';

export default function initializeChron() {
  const app = chron();

//  Object with key value pairs of rental listing urls

// every 5 minutes
    // Loop through urls
        // If not there then scrape url\
        // If delisted then delete from db

 
}